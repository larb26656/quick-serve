{
  "name": "q-serve",
  "version": "1.0.0",
  "description": "CLI tool for serving static files temporarily with auto-expiration",
  "type": "module",
  "bin": {
    "q-serve": "./dist/index.js"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run src/index.ts",
    "serve": "bun run src/index.ts server",
    "test": "bun test",
    "build": "bun build ./src/index.ts --outdir ./dist --target node",
    "package": "bun run build & npm install -g ."
  },
  "dependencies": {
    "commander": "^12.0.0",
    "elysia": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
